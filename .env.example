# Copie este arquivo para .env (que fica de fora do git/docker) e preencha
# com valores reais antes de rodar `docker compose up -d --build`.

DASHBOARD_USER=admin
DASHBOARD_SENHA=troque-esta-senha
DASHBOARD_PORT=5000
